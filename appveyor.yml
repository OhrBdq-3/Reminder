image: Visual Studio 2022  # 使用云端预装好的环境
stack: python 3.10
install:
  - pip install flet
build_script:
  - flet build windows      # 在云端执行打包
artifacts:
  - path: build/windows/*.exe  # 打包完后把 exe 提取出来供你下载